# Unified local dev environment for OpenAgentic (proxy + media + remote daemon).
# Copy to `.env` and fill in secrets:
#
#   cp .env.example .env
#
# Notes:
# - This file is safe to commit. `.env` must never be committed.
# - Keep values shell-compatible (this file is sourced by `bash`).

### Godot runtime (what the game reads)
OPENAGENTIC_PROXY_BASE_URL="http://127.0.0.1:8787/v1"
OPENAGENTIC_MODEL="gpt-5.2"

OPENAGENTIC_MEDIA_BASE_URL="http://127.0.0.1:8788"
OPENAGENTIC_MEDIA_BEARER_TOKEN="dev-token"
OPENAGENTIC_MEDIA_CACHE_MAX_BYTES="104857600" # 100 MiB (optional)
OPENAGENTIC_MEDIA_CACHE_TTL_SEC="0"           # 0 = disabled (optional)

### Launcher-only (ports/hosts for local services)
OPENAGENTIC_PROXY_HOST="127.0.0.1"
OPENAGENTIC_PROXY_PORT="8787"

OPENAGENTIC_MEDIA_HOST="127.0.0.1"
OPENAGENTIC_MEDIA_PORT="8788"
OPENAGENTIC_MEDIA_STORE_DIR="/tmp/oa-media"
OPENAGENTIC_MEDIA_STORE_MAX_BYTES="536870912" # 512 MiB (optional)
OPENAGENTIC_MEDIA_STORE_TTL_SEC="0"           # 0 = disabled (optional)

### Proxy upstream (required for proxy)
# Get your key from your OpenAI account dashboard.
OPENAI_API_KEY=""
OPENAI_BASE_URL="https://api.openai.com/v1"

### Tavily (WebSearch tool)
TAVILY_BASE_URL="https://api.tavily.com"
TAVILY_API_KEY=""

### Rust remote daemon (optional; connects to an IRC server)
OA_IRC_HOST="127.0.0.1"
OA_IRC_PORT="6667"
OA_IRC_PASSWORD=""
OA_IRC_NICK=""
OA_IRC_USER=""
OA_IRC_REALNAME="OpenAgentic Remote"

OA_REMOTE_POLL_SECONDS="30"
OA_REMOTE_INSTANCE="default"
OA_REMOTE_DATA_HOME=""

# DANGER: enables real `bash` execution by remote daemon.
OA_REMOTE_ENABLE_BASH="0"
OA_REMOTE_BASH_TIMEOUT_SEC="30"

### Python media sender (optional; defaults used by scripts/oa_media_sender*.py)
OA_SENDER_IRC_CHANNEL="#test"
OA_SENDER_IRC_NICK="oa_sender"
OA_SENDER_IRC_MAX_LEN="360"
