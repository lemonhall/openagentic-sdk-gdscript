# v55 index

Goal: make `MediaFetch` / `MediaUpload` automatically pick up VR Offices per-save media config (Settings → Media), so the LLM/tool path works without requiring environment variables.

## Artifacts

- Plan: `docs/plan/v55-media-tools-read-per-save-config.md`

