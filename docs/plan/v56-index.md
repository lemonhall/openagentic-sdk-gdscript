# v56 index

Goal: after a successful `MediaFetch`, the next user turn should carry the fetched image into the OpenAI Responses request as an `input_image` part (so the model can use native vision instead of trying `Read`/`RemoteBash`).

## Artifacts

- Plan: `docs/plan/v56-inject-recent-mediafetch-image-into-responses.md`

